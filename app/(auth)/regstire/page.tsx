import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Login",
};
const RegistrePage = () => {
    return (
        <div>
            Hello Login
        </div>
    );
}

export default RegistrePage;
